
// 1- Write a program in JavaScript to check the user enter input or not?

    // var input = prompt("enter  input? ");
    // if (input === null) {
    //     document.writeln("enter input pls");

    // }
    // else {
    //     document.writeln("renter correct input pls");

    // }

   // 2- Write a program in JavaScript to check if a number is within a specified range (e.g., 1 to 100).

//    var number =+prompt("enter  number within a specified range (e.g., 1 to 100).");
//    if (number >=1 && number<=100) {
//        document.writeln("number in range ");
//    }
//    else {
//        document.writeln("number is not in the range");
//    }
   //3.Write a program in JavaScript to check if a person is eligible to vote (age >= 18).

   var age =+prompt("Enter  age of the person is eligible to vote.age >= 18");
   if (age >=18 ) {
       document.writeln("person is eligible to vote ");
   }
   else {
       document.writeln("person is not eligible to vote");
   }
